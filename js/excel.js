window.ExcelUtil={cleanHeader:v=>String(v??"").trim(),headerKey:v=>String(v??"").trim().toLocaleLowerCase("es-AR").normalize("NFD").replace(/[\u0300-\u036f]/g,""),value:v=>(v==null?"":String(v).trim()),readWorkbook(file){return new Promise((resolve,reject)=>{const r=new FileReader();r.onload=e=>{try{const w=XLSX.read(new Uint8Array(e.target.result),{type:"array",cellDates:true}),s=[];w.SheetNames.forEach(n=>{const rows=XLSX.utils.sheet_to_json(w.Sheets[n],{header:1,defval:"",raw:false});if(!rows.length)return;const hs=[],seen=new Set();(rows[0]||[]).forEach((h,i)=>{const label=ExcelUtil.cleanHeader(h),key=ExcelUtil.headerKey(label);if(label&&!seen.has(key)){seen.add(key);hs.push({label,key,index:i})}});const data=[];for(let i=1;i<rows.length;i++){const o={};hs.forEach(h=>o[h.key]=ExcelUtil.value((rows[i]||[])[h.index]));if(Object.values(o).some(Boolean))data.push(o)}if(hs.length)s.push({name:n,headers:hs,data})});resolve({name:file.name,sheets:s})}catch(x){reject(x)}};r.onerror=()=>reject(new Error("No se pudo leer el archivo."));r.readAsArrayBuffer(file)})}};};